#this function gets executed every time a new player joins the server.

#you can use it to give specific players the "admin" tag upon joining the server.
#/tag doesn't work on players who are offline, so that's why this function exists.

#example:
tag _SAMPLE_USERNAME add admin

#add more here
#...