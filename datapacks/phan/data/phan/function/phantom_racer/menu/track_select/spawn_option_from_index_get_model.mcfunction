#$(level_id) = uid of the dream we want the preview image of

#just grab the image for act 1. let's not think too hard about this.
$data modify storage phan:level_index model set from storage phan_dream_$(level_id):pr_act_1 thumbnail_text